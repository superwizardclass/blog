---
import Layout from "../layouts/Layout.astro";
const posts = await Astro.glob("../pages/posts/*.md");
---

<Layout title="posts">
  <h1 class="text-lg">Archive</h1>
  <section class="flex flex-col">
    {posts.map((post) => <a href={post.url}>{post.frontmatter.title}</a>)}
  </section>
</Layout>
